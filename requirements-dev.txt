# Development & testing dependencies
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0.0
pytest-cov>=4.0.0
ruff>=0.5.0

# Agent evaluation (LLM-as-judge). Eval tests skip when no API key is set.
deepeval>=3.0.0
